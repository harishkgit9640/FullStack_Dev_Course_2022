/*
let arr = [-5, -4, -3, -2, 0, 2, 4, 6];

let zeroSum = (arr) => {
    for (const number of arr) {
        for (i = 1; i < arr.length; i++) {
            if (number + arr[i] === 0) {
                return [number, arr[i]];
            }
        }
    }
}
console.log(zeroSum(arr));
*/

